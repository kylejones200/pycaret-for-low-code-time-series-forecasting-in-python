data:
  source: null
  date_column: "date"
  value_column: "value"
  generate_synthetic: true
  n_periods: 200
  seed: 42

pycaret:
  train_size: 0.8
  compare_models: true
  create_model: "arima"
  tune_model: false

output:
  figures_dir: "images"
  figure_format: "png"
  figure_dpi: 100

